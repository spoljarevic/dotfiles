# Productivity layout. Launch from any pane with:
#   kitty --detach --session /home/ls/git/ls/Dotfiles/kitty/session-productivity.conf
# See howto.md for details.
#
# Layout:
#   +------------------------+
#   |         nvim           |  ~65%
#   +------------+-----------+
#   |   shell    |  claude   |  ~35%
#   +------------+-----------+

new_tab productivity
layout splits
launch --title nvim --cwd=current nvim
focus
launch --title shell --location=hsplit --bias=35 --cwd=current
launch --title claude --location=vsplit --cwd=current claude
